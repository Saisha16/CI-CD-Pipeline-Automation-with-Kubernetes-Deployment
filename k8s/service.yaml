apiVersion: v1
kind: Service

metadata:
  name: app-service
  namespace: __KUBE_NAMESPACE__

spec:
  selector:
    app: cicd
    version: blue

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080

  type: NodePort
