apiVersion: apps/v1
kind: Deployment

metadata:
  name: app-green
  namespace: devops

spec:
  replicas: 2

  selector:
    matchLabels:
      app: cicd
      version: green

  template:
    metadata:
      labels:
        app: cicd
        version: green

    spec:
      containers:
      - name: app
        image: cicd-app:20260410144325
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "200m"
          limits:
            cpu: "500m"
