apiVersion: v1
kind: Service

metadata:
  name: app-service
  namespace: devops

spec:
  selector:
    app: cicd
    version: blue

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080

  type: NodePort
